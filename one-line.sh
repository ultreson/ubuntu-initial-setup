#!/bin/bash
sudo apt update && sudo apt dist-upgrade && sudo apt install -y vim curl python-pip yum xbacklight && git config --global user.name "ultreson" && git config --global user.email "ultreson@outlook.com" && git config --global core.editor code
